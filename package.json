{
  "name": "stage4",
  "version": "1.0.0",
  "description": "1- Fetching data from refuge api\r Using axios we fetch information about toilets from refuge api.\r We then store that information in JSON files to have the data we need to create our documents in our collections.\r Because I am using fs.writeFile(), which is going to override anything previusly stored in the JSON files on our side,\r if the data from refuge api is updated in any shape or form, it is going to be reflected in our JSON files updating our files\r with the latest updates on refuge api.",
  "main": "fetchData.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@shelf/jest-mongodb": "^4.2.0",
    "axios": "^1.6.7",
    "dotenv": "^16.4.4",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.1.3",
    "node-schedule": "^2.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  }
}
